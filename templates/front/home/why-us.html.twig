<section id="why-us" class="why-us">
    <div class="container">

        <div class="row">
            <div class="col-lg-4 d-flex align-items-stretch">
                <div class="content">
                    <h3>{{ content.boxOneTitle|raw }}</h3>
                    <p>{{ content.boxOneContent|raw }}</p>
{#                    <div class="text-center">#}
{#                        <a href="#services" class="more-btn">En savoir plus <i class="bx bx-chevron-right"></i></a>#}
{#                    </div>#}
                </div>
            </div>
            <div class="col-lg-8 d-flex align-items-stretch">
                <div class="icon-boxes d-flex flex-column justify-content-center">
                    <div class="row">
                        <div class="col-xl-4 d-flex align-items-stretch">
                            <div class="icon-box mt-4 mt-xl-0">
                                <i class="bx bx-receipt"></i>
                                <h4>{{ content.boxTwoTitle|raw }}</h4>
                                <p>{{ content.boxTwoContent|raw }}</p>
                            </div>
                        </div>
                        <div class="col-xl-4 d-flex align-items-stretch">
                            <div class="icon-box mt-4 mt-xl-0">
                                <i class="bx bx-cube-alt"></i>
                                <h4>{{ content.boxThreeTitle|raw }}</h4>
                                <p>{{ content.boxThreeContent|raw }}</p>
                            </div>
                        </div>
                        <div class="col-xl-4 d-flex align-items-stretch">
                            <div class="icon-box mt-4 mt-xl-0">
                                <i class="bx bx-images"></i>
                                <h4>{{ content.boxFourTitle|raw }}</h4>
                                <p>{{ content.boxFourContent|raw }}</p>
                            </div>
                        </div>
                    </div>
                </div><!-- End .content-->
            </div>
        </div>

    </div>
</section>